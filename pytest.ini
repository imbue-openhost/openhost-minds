; Root guard only: keeps a bare `pytest` from collecting the template
; submodule's suites. The real tests are their own uv project — run them with
; `cd tests/openhost && uv run pytest`.
[pytest]
testpaths = tests/openhost
norecursedirs = openhost_minds_template .git
